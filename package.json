{
  "name": "valund-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build",
    "test": "pnpm --filter frontend test"
  },
  "devDependencies": {
    "@types/react": "^19.1.17",
    "@types/react-dom": "^19.1.11"
  }
}
